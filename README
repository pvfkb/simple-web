This is a simple web.py app for testing stuff out.

simple-web.sh is a daemon script that can be implemented on Ubuntu (or similar Linux distro) by copying to /etc/init.d/ 
and running sudo update-rc.d simple-web.sh defaults
